export const minWindowsConfig = {
  width: 900,
  height: 670,
  resizable: false, // 禁止手动调整宽高
  show: false, // 是否在创建时立即显示
  frame: false, // 设置无边框
  transparent: true, // 设置窗口透明
  backgroundColor: '#00000000'
}
export const otherWindowsConfig = {
  width: 1024,
  height: 480,
  resizable: false, // 禁止手动调整宽高
  show: false, // 是否在创建时立即显示
  frame: false, // 设置无边框
  transparent: true, // 设置窗口透明
  backgroundColor: '#00000000'
}
export const videoWindowsConfig = {
  width: 450,
  height: 800,
  resizable: false, // 禁止手动调整宽高
  show: false, // 是否在创建时立即显示
  frame: false, // 设置无边框
  transparent: true, // 设置窗口透明
  backgroundColor: '#00000000'
}
export const remoteReceiverWindowsConfig = {
  width: 350,
  height: 100,
  alwaysOnTop: true,
  resizable: false, // 禁止手动调整宽高
  show: false, // 是否在创建时立即显示
  frame: false, // 设置无边框
  transparent: true, // 设置窗口透明
  backgroundColor: '#00000000'
}
export const remoteSenderWindowsConfig = {
  width: 450,
  height: 800,
  resizable: false, // 禁止手动调整宽高
  show: false, // 是否在创建时立即显示
  frame: false, // 设置无边框
  transparent: true, // 设置窗口透明
  backgroundColor: '#00000000'
}
